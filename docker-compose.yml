version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./logs:/app/logs
      - ./outputs:/app/outputs
      - ./uploads:/app/uploads
    environment:
      - PYTHONUNBUFFERED=1
